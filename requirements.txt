# Core
numpy>=1.24.0
pandas>=2.0.0
scipy>=1.11.0
matplotlib>=3.7.0
torch>=2.1.0
nflows>=0.14

# Data build (data/extend_to_1971.py)
yfinance>=0.2.30
pandas-datareader>=0.10.0

# Optional — baselines / encoder ablation only
arch>=6.0            # GARCH baseline (train_garch_xpast.py)
# mamba-ssm>=1.2     # Mamba encoder ablation (run_rawvol_macroenc.py); needs CUDA build
# causal-conv1d>=1.2 # companion of mamba-ssm
